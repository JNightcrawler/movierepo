import { TablecolDirective } from './tablecol.directive';

describe('TablecolDirective', () => {
  it('should create an instance', () => {
    const directive = new TablecolDirective();
    expect(directive).toBeTruthy();
  });
});
